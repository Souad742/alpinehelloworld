
            
pipeline {
    agent any

    environment {
        SONARQUBE_URL = 'http://localhost:9000'  // URL de ton SonarQube local
        SONARQUBE_TOKEN = credentials('alpinehelloworld')  // Utiliser le credential 'alpinehelloworld'
    }

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/Souad742/alpinehelloworld.git'
            }
        }

        stage('Build') {
            steps {
                script {
                    sh 'mvn clean install'
                }
            }
        }

        stage('SonarQube Analysis') {
            steps {
                script {
                    sh """
                        mvn sonar:sonar \
                            -Dsonar.projectKey=alpinehelloworld \
                            -Dsonar.projectName=AlpineHelloWorld \
                            -Dsonar.projectVersion=1.0 \
                            -Dsonar.sources=. \
                            -Dsonar.java.binaries=target/classes \
                            -Dsonar.host.url=${SONARQUBE_URL} \
                            -Dsonar.login=${SONARQUBE_TOKEN}
                    """
                }
            }
        }
    }

    post {
        always {
            echo 'Pipeline finished'
        }
        success {
            echo 'Build and SonarQube analysis completed successfully!'
        }
        failure {
            echo 'Pipeline failed!'
        }
    }
}

