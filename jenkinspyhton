pipeline {
    agent {
        docker {
            image 'python:3.11' // Image Docker avec Python préinstallé
        }
    }

    environment {
        SONARQUBE = 'SonarQube' // Nom de ton serveur SonarQube configuré dans Jenkins
    }

    stages {
        stage('Checkout') {
            steps {
                echo '📥 Récupération du code source...'
                checkout scm
            }
        }

        stage('Check Python and pip') {
            steps {
                echo '🔍 Vérification de Python et pip...'
                sh 'python --version'
                sh 'pip --version'
            }
        }

        stage('Install Dependencies') {
            steps {
                echo '📦 Installation des dépendances Python...'
                sh '''
                    if [ -f requirements.txt ]; then
                        pip install -r requirements.txt
                    else
                        echo "Pas de fichier requirements.txt trouvé, on passe cette étape."
                    fi
                '''
            }
        }

        stage('Run Script') {
            steps {
                echo '🚀 Exécution de ton script Python...'
                sh 'python ton_script.py'
            }
        }

        stage('SonarQube Analysis') {
            steps {
                echo '🔎 Analyse du code avec SonarQube...'
                script {
                    def scannerHome = tool 'SonarQube Scanner' // Configuré dans Jenkins > Global Tool Configuration
                    withSonarQubeEnv("${SONARQUBE}") {
                        sh """
                            ${scannerHome}/bin/sonar-scanner \
                                -Dsonar.projectKey=alpinehelloworld \
                                -Dsonar.sources=. \
                                -Dsonar.python.version=3.11
                        """
                    }
                }
            }
        }

        stage('Build') {
            steps {
                echo '🔧 Étape de build (optionnelle si ton projet n\'a pas de setup.py)...'
                sh '''
                    if [ -f setup.py ]; then
                        python setup.py install
                    else
                        echo "Pas de setup.py trouvé, build ignoré."
                    fi
                '''
            }
        }
    }

    post {
        success {
            echo '✅ Build et analyse SonarQube terminés avec succès.'
        }
        failure {
            echo '❌ Une étape du pipeline a échoué.'
        }
    }
}
